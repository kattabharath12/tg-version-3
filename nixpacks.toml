[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "bash start-with-migrate.sh && npm start"
